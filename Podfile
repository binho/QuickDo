platform :ios, '9.0'

target 'QuickDo' do
        use_frameworks!

        pod 'RealmSwift'
        pod 'JTAppleCalendar', '~> 6.0'
        pod 'Jelly'
        pod 'Malert'
        pod 'TDBadgedCell' # https://github.com/tmdvs/TDBadgedCell
        pod 'BWSwipeRevealCell' # https://github.com/bitwit/BWSwipeRevealCell

        # target 'QuickDoTests' do
        # 		use_frameworks!
        #         inherit! :search_paths
        #         # Pods for testing
        # end

end

#post_install do |installer|
#       installer.pods_project.targets.each do |target|
#               target.build_configurations.each do |config|
#                       config.build_settings['SWIFT_VERSION'] = '3.0'
#               end
#       end
#end
